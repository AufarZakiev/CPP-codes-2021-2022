#include <algorithm>
#include <iostream>
#include <vector>

struct User {
  int age;
  std::string name;
};

int main() {
  std::vector<User> users{{26, "Vanya"}, {18, "Zohan"}};
  // live write std::sort
}